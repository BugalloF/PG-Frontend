import { Link } from "react-router-dom";
import React from "react";

const Card = ({image, userId, postId}) => {
  //vamos a hacer un llamado a la API, usando la userId, y guardando eso en una constante json


  //en el return, vamos a verificar que la constante json del usuario tenga datos, asi renderiza la carta, si no, no la renderiza aun
  return (
    <div className="Card">

    </div>
  );
};
export default Card;